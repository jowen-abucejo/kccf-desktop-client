import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kccf-desktop-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
